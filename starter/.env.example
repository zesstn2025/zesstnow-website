# Where enquiries are emailed. Without these the form still works — it falls
# back to a WhatsApp link carrying the same message, so nothing is lost.
ENQUIRY_TO=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Used by sitemap.xml and robots.txt.
SITE_URL=https://example.com
